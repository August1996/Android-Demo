<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.august1996.top.databingdingusage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DataBind2ViewActivity" />
        <activity android:name=".activity.ViewBind2DataAndDataBind2ViewActivity" />
        <activity android:name=".activity.ListViewActivity" />
        <activity android:name=".activity.ImportViewActivity"></activity>
    </application>

</manifest>